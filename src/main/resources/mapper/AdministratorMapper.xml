<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mapper.AdministratorMapper">

    <select id="getAdministratorById" resultType="com.bean.Administrator" parameterType="java.lang.Long">
        select *
        from administrator
        where a_id = #{id};
    </select>

    <insert id="addEmployee" parameterType="com.bean.Employee" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        <!--主键自增-->
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
            SELECT LAST_INSERT_ID() as id;
        </selectKey>
        insert into
            employee
        values
            (#{id}, #{name}, #{gender}, #{age}, #{time}, #{address}, #{telephone}, #{email}, #{dept});
    </insert>

    <update id="updateCourse" parameterType="com.bean.Course">
        update
            course
        set
            course_name = #{course_name}, instructor_id = #{instructor_id}, type = #{type}, content = #{content}
        where
            course_id = #{course_id};
    </update>

    <insert id="addLog" parameterType="com.bean.Log">
        <selectKey keyProperty="log_id" order="AFTER" resultType="int">
            SELECT LAST_INSERT_ID() as log_id;
        </selectKey>
        insert into
            log
        values
            (#{log_id}, #{employee_id}, #{operation}, #{date});
    </insert>

    <select id="queryAllEmployee" resultType="com.bean.Employee">
        select *
        from employee;
    </select>

    <select id="queryAllCourse" resultType="com.bean.Course">
        select *
        from course;
    </select>

    <select id="queryAllLog" resultType="com.bean.Log">
        select *
        from log;
    </select>

</mapper>